This example should be run with only three modules, it is not intended to run with less or more modules.

In this example, the two follower modules exchange memory slots after every iteration, generating interweaving fizzbuzz values.