syntax = "proto3";
package rofi.messages;

import "jointCmd.proto";
import "jointCapabilities.proto";

message JointResp {
    int32 joint = 1;
    JointCmd.Type respType = 2;

    JointCapabilities capabilities = 3;
    float value = 4;
}
