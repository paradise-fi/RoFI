syntax = "proto3";
package rofi.messages;

message DistributorReq {
    enum Type {
        GET_INFO = 0;
        LOCK_ONE = 1;
        TRY_LOCK = 2;
        UNLOCK = 3;
    }

    bytes sessionId = 1;
    Type reqType = 2;
    int32 rofiId = 3;
}
